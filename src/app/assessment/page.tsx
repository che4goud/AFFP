import QuestionFlow from "@/components/assessment/QuestionFlow";

export const metadata = {
  title: "Assessment — Armed Forces Financial Risk Assessment",
};

export default function AssessmentPage() {
  return <QuestionFlow />;
}
